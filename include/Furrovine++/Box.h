#pragma once

#include <Furrovine++/TBox.h>

namespace Furrovine {

	typedef TBox<int> Box;
	typedef TBox<int> Boxi;
	typedef TBox<uint> Boxu;
	typedef TBox<int32> Box32;
	typedef TBox<uint32> Boxu32;
	typedef TBox<float> Boxf;
	typedef TBox<double> Boxd;

}
