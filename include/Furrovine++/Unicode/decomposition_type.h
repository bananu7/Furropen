#pragma once

namespace Furrovine { namespace Unicode {

	enum class decomposition_type : unsigned char {
		can, com, enc, fin, font, fra,
		init, iso, med, nar, nb, sml,
		sqr, sub, sup, vert, wide, none
	};

}}