#pragma once

#include <Furrovine++/Strings.h>

namespace Furrovine { namespace Shaders { namespace detail {
	
	furrovineapi const String& NymphShaderSource( );

	furrovineapi const String& ImageShaderSource( );

	furrovineapi const String& QuadShaderSource( );

}}}